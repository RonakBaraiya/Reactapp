# Reactapp
React app that manages registered accounts smoothly.
## Project Setup

1. Clone the repo
2. Run `npm install`
3. Run `npm start`

## Description
Simple and interactive React login & register app using localStorage.
